# event_scrapping
A automated system that scrapes real-world events (not movies) from event platforms, removes duplicates, and stores the data — then syncs it to Google Sheets and displays live analytics on a publicly hosted dashboard.
